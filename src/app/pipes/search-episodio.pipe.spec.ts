import { SearchEpisodioPipe } from './search-episodio.pipe';

describe('SearchEpisodioPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEpisodioPipe();
    expect(pipe).toBeTruthy();
  });
});
