import { SearchHipodromoPipe } from './search-hipodromo.pipe';

describe('SearchHipodromoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHipodromoPipe();
    expect(pipe).toBeTruthy();
  });
});
